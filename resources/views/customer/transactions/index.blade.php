@extends('customer.layout') @section('title','Transactions') @section('content')
<h1>Transaction history</h1><table class="table"><thead><tr><th>Reference</th><th>Type</th><th>Status</th><th>Amount</th><th>Created</th></tr></thead><tbody>@forelse($transactions as $transaction)<tr><td><a href="{{ route('customer.transactions.show',$transaction) }}">{{ $transaction->reference }}</a></td><td>{{ $transaction->type->value }}</td><td><span class="badge {{ $transaction->status->value }}">{{ $transaction->status->value }}</span></td><td>{{ $transaction->currency_code }} {{ number_format($transaction->amount_minor / (10 ** $transaction->currency->minor_unit), $transaction->currency->minor_unit) }}</td><td>{{ $transaction->created_at }}</td></tr>@empty<tr><td colspan="5">No transactions yet.</td></tr>@endforelse</tbody></table><div style="margin-top:16px">{{ $transactions->links() }}</div>
@endsection
